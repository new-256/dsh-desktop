# DSH Desktop 更新日志

这里记录每个版本改了什么。版本号和 `package.json` 保持一致。

## 0.3.34（2026-09-10）

**修"插件加载 431"和"装了 0.3.33 还是起不来"两件事，顺手把手机访问的网络结构改安全了。**

1. **HTTP 431 修复**：插件装多了之后（本机 49 个），页面加载插件用的组合脚本 URL 超过 Node 默认 16KB 请求头上限，请求直接被服务器以 431 拒掉，表现为"插件全加载失败"。现在启动后端时带上 `--max-http-header-size=1048576`（1MB），这个参数走的是 Node 运行时旗标，后端换版本也不会丢。
2. **手机访问改成本地桥接**：原来开启手机访问后，后端会直接绑 `0.0.0.0`（整个局域网都能直连后端端口）；现在后端永远只绑 `127.0.0.1`，由桌面壳在你配置的手机端口上做一层 TCP 转发桥（局域网端口 → 本机后端端口），同时带 `--trusted-host` 放行局域网 IP 的 Host 头、自动补防火墙规则。攻击面小了，手机端用法不变。
3. **网络诊断日志**：主窗口的网络请求错误、`/plugins/` 响应状态码、渲染层 console 输出全部落桌面日志——以后"页面白屏、插件加载失败"这类问题不用再靠猜。
4. **版本整合（本版重发的直接原因）**：0.3.33 的安装包构建于 431 修复之前；而 0.3.33 发布后，另一个修复进程又直接热修了已安装目录里的 asar，造成"main.js 是新的、updater-backend.js 还是旧的、版本号停在 0.3.32"的混合体——启动即报 `validateAndRepairPatchLayers is not a function`（0.3.33 的预检函数在旧库里不存在）。本版把两边的修复合成一个连贯构建，重装即愈。

## 0.3.33（2026-09-10）

**修一条"自救按钮把自己锁死在门外"的死循环。** 9/9 深夜那次启动失败，链条是这样的：用户从托盘点「以安全模式重启」→ 安全模式把家级补丁的每一行都注释掉，但**忘了在文件末尾留下 `[]`** → 文件只剩注释，YAML 解析成 `null` → 上游 loader 在加载任何插件之前就抛 `patches ... must be a top-level YAML array of loader patch entries` 直接退出 → 自愈三级（隔离 profile、禁用条目、重下 Node）全部空转，因为它们没有一级是针对"文件结构非法"的 → 最后弹个错误框 `app.quit()`。而托盘图标是在启动流程第 6 步（后端起来之后）才创建的，这次根本没走到，所以**用户面前只有一个错误框，没有托盘、没有安全模式、没有任何入口**——唯一的自救按钮正是把自己害死的那个。

这版改了四处：

1. **注释补丁时必须留下 `[]`**。`commentPatchEntries` / `commentPatchBlocks` 全注释后自动补空数组，`writePatchText` 作为总闸再兜一次——以后任何调用方都不可能再写出非法文件。安全模式、插件隔离轮测走的都是这条路。
2. **启动预检查结构合法性**（在 ID 冲突扫描之前，因为文件结构不合法时后面的解析都没意义）。发现损坏按三级修复：只剩注释就补 `[]`（**注释掉的配置一行不动**）→ 结构真坏了就回退到最近一个**校验通过**的备份 → 都不行才重置为空补丁层（原文件完整备份）。
3. **自愈新增结构修复级**（0a，排在所有其他级之前），处理运行中被写坏的情况；日志里报了结构错误但当前文件校验通过时，直接重试而不是空跑一遍阶梯。
4. **托盘提前到启动最开始创建**，并按阶段切换菜单：启动中显示「● 正在启动后端…」，失败时变成完整的自救台——查看启动日志 / 查看错误清单 / 打开日志目录 / 重试启动 / 以安全模式重启 / 修复插件配置文件 / 打开数据目录。`showFatal` 也不再直接退出：托盘还活着就常驻，错误框给出六个可执行选项（查看日志、修复配置、安全模式、重试、留在托盘、退出），关掉框还能从托盘再进。托盘创建失败时才回退到旧的"弹框退出"。

回归验证 28 项全过，含用真实事故文件复现的端到端修复（56 行配置全部保留、profile 补丁未被误动、修复后复检零问题）。

## 0.3.32（2026-09-09）

**修复"命令行装插件会装错地方"这一结构性问题。** 根因：桌面版把后端隔离在自己的 `DSH_HOME`（`userData\dsh-home`，`buildDedicatedEnv()` 会主动剥离继承的 `DSH_HOME`），而用户在普通终端里跑的 `dsh`（npm 全局装）默认 home 是 `~/.dsh`。照上游 README 直接敲 `dsh plugin --profile web add X`，插件会装进旧 home，桌面版永远加载不到——本机 `~/.dsh` 里 9 个插件（含 `dsh-chrome@0.1.3`，即 `browser_*` 工具不出现的原因）就是这样产生的。**注意 `dsh plugin add` 写的是 `package.json` 的 `dependencies` + `dsh.profile.bundles`（后者才决定是否被加载），不是 `cordis.patch.yml`——手工拷包目录无效。**

设置窗新增「**插件安装与终端**」区，三条根治通道 + 一条止梗通道：

1. **应用内安装（推荐，永不装错）**：填包名（`dsh-chrome` / `dsh-chrome@0.1.3` / 本地 `.tgz` 路径）点安装，主进程用桌面版自带的 Node + dsh 入口、经 `buildDedicatedEnv()`（`DSH_HOME` 天然正确）执行 `plugin add`，安装日志实时回显，成功后提示重启加载。分发用户无需安装 CLI、无需碰终端、不可能装错。
2. **终端包装器**：自动生成 `dsh-desktop.cmd` / `dsh.cmd` / `dsh-desktop.ps1` 到 `userData\bin`（每次启动刷新），脚本内固定 `DSH_HOME` + 调用桌面版 Node 与 dsh 入口，并带"后端未初始化"守卫。可一键加入用户 PATH——**追加在末尾，不抢占**用户已有的全局 `dsh`。终端里 `dsh-desktop plugin --profile web add X` 永远正确。
3. **智能 `DSH_HOME` 环境变量**：写用户级（`HKCU\Environment`）`DSH_HOME` 并广播 `WM_SETTINGCHANGE`，让 README 原文命令也落到桌面 home。**智能默认**：检测到 `~/.dsh` 已在使用（有插件/会话/凭据）时**不自动写入**，避免让原有独立 CLI 环境"消失"，仅在设置窗给开关；干净机器首次启动自动写入，分发用户开箱即对。决策记在 `settings.json`，只做一次，用户关掉后不会被重新打开。
4. **旧 Home 扫描 + 勾选导入**：对比两个 home 的 `dsh.profile.bundles`，列出桌面版看不到的插件（名称/版本/形态），勾选后逐个走通道 ① 的正规流程装进桌面 home；`link:`/`file:` 本地依赖标记为"需手工"不盲目重装；结果导出 `DSH-Desktop-插件迁移报告-<时间戳>.txt` 到桌面。

修复：0.3.31 引入的设置窗 DOM 缺陷——「DSH 后端版本管理」区缺少闭合 `</div>`，导致「手机访问」区被错误嵌套其中（现已配平，6 个区块各自独立）。

验证：真实环境 25 项断言全过（旧 home 扫描识别 9 个孤儿并读到真实版本、link 依赖正确标记、包装器三脚本内容与守卫、PATH/环境变量只读状态、迁移报告内容、空包名拒绝）；并完成**真实端到端安装**——经应用内通道装入 `dsh-chrome@0.1.3` 后 `bundles` 已含 `dsh-chrome`、包目录与版本正确；包装器在**故意不带 `DSH_HOME` 的干净终端**中执行 `plugin list`，正确解析到桌面版 home 并列出该插件。安全守卫复核：本机（旧 home 在用）`preseed` 正确跳过，未改动任何环境变量与 PATH。

## 0.3.31（2026-09-09）

版本管理增强（设置窗）——Node 与 DSH 后端都支持"任意版本选择/回退"，后端回退带插件兼容性分析并导出桌面报告：

1. **Node 任意版本（含回退）**：设置窗「Node.js 基座更新」新增版本下拉，列出同主版本（24.x、与原生模块 ABI 对齐）且满足 DSH 最低要求（≥22.15.0）的全部版本；选择旧版即可回退（下载暂存 + 重启生效），选非法版本会被拒绝。新增 `listNodeVersions()` / `stageNode(callbacks, requestedVersion)`。
2. **后端全版本选择（安装与运行期）**：设置窗新增「DSH 后端版本管理」区块：安装后随时拉出 npm 已发布的**全部 18 个版本**（倒序，标注当前/最新），选定目标可「下载并切换」（升级或回退均走暂存+重启，回退自带 dsh.previous 兜底）。安装程序新增「在线自选版本（安装后在设置窗选择全部版本）」选项，并刷新了内置的便捷版本号。
3. **engines 声明检查（Node 联动提醒）**：检查更新时抓取目标后端声明的 Node 要求（`engines.node`，缺失时兜底 ≥22.15.0），设置窗「更新设置」区显示"后端 Node 要求 ≥X（当前…满足/不满足）"；不满足时红色警告并提示**先更新 Node 再更新后端**。新增 `backendEnginesFor(version)`。
4. **后端回退插件兼容分析 + 桌面导出**：选定目标版本后一键「分析兼容性并导出桌面报告」——逐插件给出**可用/不可用/未验证**三档判定（源文件缺失→不可用；插件声明 dsh 版本约束且目标不满足→不兼容；无声明约束且非当前版本→未验证，诚实提示），并在桌面生成 `DSH-Desktop-回退兼容报告-<目标版本>-<时间戳>.txt`（含目标/当前版本、Node 要求、逐插件判定与原因）。裸名插件按真实目录或 junction 解析（兼容 2026-09-08 布局修复后的存放形态，不再误报缺失）。
5. 兼容性分析对真实环境 7 项断言全过（全版本列表/Node 可回退列表/engines 兜底/真实插件分级/旧版本未验证分级/非法 Node 版本拒绝/报告导出与内容）。离线安装包仍只内置默认后端版本（在线选择覆盖全部版本，安装包大小不变）。

## 0.3.30（2026-09-09）

Electron 大版本升级 + 构建/更新链配套升级，清零 dependabot 告警的主体来源（Electron 33 线早已 EOL，34 之后修复的 Chromium CVE——含 CVE-2025-10585、CVE-2025-2783 等在野利用项——全部随本次升级消除）：

1. **Electron 33.4.11 → 44.3.0**（Chromium 130→152、Node 20→24、V8 13→15）：逐代审计 34→44 breaking changes 后确认本项目代码无需改动——`app.commandLine` 两个开关本就全小写（36 开关归一化无影响）、dialog 只用错误框（43 默认目录变化无影响）、仅 x64 打包（44 移除 ia32 无影响）、主进程无原生模块（Node ABI 变化无影响）。
2. **electron-builder 25.1.8 → 26.15.3**：① 26 默认 makensis 警告即错误（`-WX`），其新增的 PowerShell 检测模板有一处良性告警，配置 `nsis.warningsAsErrors: false` 还原原行为；② 26 移除 7zip-bin npm 包，构建所需 7za 改为工具集自下载（7-Zip 24.09，已修 CVE-2024-11477/CVE-2025-0411）；`build/payload/7za.exe` 改为自包含文件（更新器解压后端 7z 用，不再依赖 node_modules）。
3. **electron-updater 6.3.9 → 6.8.9**：经依赖携带 builder-util-runtime 9.7.0，修复 CVE-2026-54673（跨源重定向泄漏凭据头，GHSA-p2f4-r6v6-j797）；6.8.8 起的更新流程 PATH/路径穿越加固一并收入。
4. **Electron 42+ 二进制懒下载适配**：postinstall 不再下载二进制，首次构建前经国内镜像 `npx install-electron` 预下载（已验证 234.7MB 拉取与运行正常）。

## 0.3.29（2026-09-09）

日志与维护体验这轮收口，外加设置窗补上桌面版版本号：

1. **日志集中管理，离开桌面**：诊断日志从桌面挪进应用自己的目录集中存放——打包后优先落在安装目录 `logs\`（不可写时自动回退到用户数据目录 `logs\`），开发态（node/electron 解释器）自动跳过安装目录探测。桌面从此只干一件事：**捕获到疑似故障时才生成 `DSH-Desktop-错误报告-<时间>.txt`**，平时绝不在桌面留文件。
2. **切分存档 + 定时清除**：主日志超 10MB 自动按时间戳切分（同秒多次触发自动加序号后缀，崩溃刷屏也不会丢块；取代旧的固定 `-旧.txt` 覆盖）；配合新增的 `DSH-Desktop-错误清单.txt`（单列疑似故障条目，超 2MB 同样切分）。每次启动和每天自动清理：主日志存档保留最近 14 份、错误清单 7 份，超过 30 天的旧档一律删除。容量阈值与保留策略都支持环境变量覆盖（`DSH_DIAG_MAX_BYTES` / `DSH_DIAG_MAX_ARCHIVES` / `DSH_DIAG_MAX_ARCHIVE_DAYS` / `DSH_DIAG_REPORT_MAX` 等）。
3. **设置窗显示桌面版版本**：更新设置区新增「桌面版」行，显示当前外壳版本（可升级时附最新版）。

19 项日志断言全过（落位/错误条目单列/桌面仅报错出报告/切分存档/同秒防覆盖/份数与天数双清理/报告大小上限）。

## 0.3.28（2026-09-09）

pet 事故的最后一课是：一个客户端插件崩，能把整个 GUI 锁死成整屏红窗，用户连设置页都进不去。这版给桌面壳加了一个**永远可用的维修通道**，顺带把 Node 基座的更新设置收进设置窗：

1. **托盘安全模式（维修通道）**：托盘菜单新增「以安全模式重启（维修）」——一键把 profile 缩到只剩核心两个 bundle、家级补丁全部注释（原样快照进 `safe-mode.json`），然后自动重启。GUI 或后端再出故障，从托盘进安全模式先把壳救起来，修好后再点托盘「退出安全模式并重启」**字节级还原**全部原配置。外壳（托盘/设置窗）本就不依赖后端，永远能点；崩溃在安全模式里也不用怕——下次正常启动会自动检测标记并还原，绝不把你晾在"精简态"里出不来。
2. **Node.js 基座更新设置**：设置窗新增「Node.js 基座更新」区——下载源可选镜像站（npmmirror，默认、国内快）或官方站（nodejs.org），带当前/最新/暂存版本显示，「检查 Node 更新」「下载并暂存 Node」两个按钮，暂存后重启生效；下载源写进设置文件，检查与下载全程跟随，环境变量 `DSH_NODE_DIST_BASE` 优先级最高。

27 项断言全过（含 0.3.27 崩溃归因、0.3.26 条目级隔离、Node 源切换、安全模式进出/崩溃自愈/字节级还原的关键路径回归）。

## 0.3.27（2026-09-09）

接着 pet 插件事故的复盘，把桌面端该扛的责任全扛了。那条事故的传导链是：插件写崩 → 整个插件树挂 → 后端起不来 → 桌面壳"整目录隔离 profiles" → 无辜的网页搜索也陪葬、全部第三方要重装。这次改成"只关肇事那一户"，三件事：

1. **先归因、后隔离**：以前启动失败先隔离整个 profiles 目录，再回头找坏插件——大部分时候根本找不到（崩溃插件匹配不上"找不到文件"模式，直接滑进大锤）。现在换序：先按日志把失败归因到具体插件条目。缺源的归缺源（原有），源码加载时崩的（SyntaxError、缺导出、import 就炸，pet 就是这类）也能归因——**只注释那一条，同批邻居照常**；只有归因彻底失败的极少数情况才走整目录隔离。
2. **隔离不再是"清了不管"**：万不得已整目录隔离前，先把每个 profile 的第三方注册（bundles、依赖、profile 补丁）快照下来；重建启动成功后**自动装回**，失败日志点名的元凶除外；装回后若再启动失败会**自动回滚**到纯净树，注册仍留在备份里供手动恢复。
3. **两处"中途夭折"兜底**：升级后的插件隔离轮测若中途崩溃（app 死在轮测中），下次启动会自动补完——已判失败的保持禁用，其余全部还原，不再出现"升级完插件全没了"；另外首次启动失败会先做一次瞬态重试（更新刚应用完、文件还没就绪就属于这类），不会直接惊动自愈机器。

29 项断言全过（含 0.3.26 条目级隔离、0.3.25 预设迁移、0.3.24 裸包名修复的关键路径回归）。pet 插件的适配问题属于插件侧，pet 0.3.2 已等价修复，桌面端不再需要拿整楼陪葬。

## 0.3.26（2026-09-09）

两件事。一是更新路线：后端组件更新一直固定走 npmmirror 镜像，9/8 晚上翻车——dsh 0.1.5-alpha.1 镜像上有，但它依赖的 dsh-fs-local@^0.1.5-alpha.1 还没同步过去，npm 直接 ETARGET，整个升级事务回滚。现在托盘里原来的「版本/更新」位置换成了「设置…」窗口：更新路线可选镜像站（国内快、同步滞后几小时到一天）或官方站（版本与依赖始终最新），选完写进设置文件，检查、下载、隔离环境里的 .npmrc 全部跟着走；真撞上 ETARGET 也不再甩一句冷冰冰的 npm 报错，而是直接告诉你"镜像还没同步完整依赖，去设置里切官方站重试"。开机自启、打开桌面日志、重启应用这些外壳配置也一并收进这个设置窗。二是插件隔离从"块级"改成"条目级"：以前更新后端后是整块整块地测，一个 `- insert:` 块里只要有一个插件挂了，整块陪葬（这就是"一棒子打死一类插件"）；现在逐条测、逐条放，坏哪条只注释哪条，同块邻居照常加载，最后弹窗也会明说"逐条检测，仅影响这些条目"。23 项断言全过（含 0.3.24/0.3.25 关键路径回归）。

## 0.3.25（2026-09-08）

傍晚又来一波：后端更新到 0.1.3-alpha.2 之后，恢复会话弹红字"preset cordis-agy failed to mount: $.prefix missing required value"——上游把 dsh-persona 的配置格式改了（老写法是 text 字段，新写法必填 prefix），自定义预设全按老格式写的，一挂载就被校验卡死，当时只能手工改文件救回来。这版把这类"上游改格式、用户配置遭殃"的事交给桌面版兜底：每次启动先看一眼自定义预设，发现还是老 text 写法、且当前后端已经是新契约（dsh-persona ≥ 0.1.3），就自动把 text 改名成 prefix——行级改写，缩进和内容一个字不动，改之前每个文件先备份，弹窗告诉你改了哪些。同一晚还有个"前端 Failed to fetch"：后端每次启动用随机端口，重启后老页面还指着旧端口发请求，当时的办法是手动 Ctrl+R。现在窗口加载失败会自动重试并自动跟到当前后端地址上（最多五次、递增退避、只管主框架），Ctrl+R 不用再按了。20 项断言全过，含 0.3.24 的关键路径复检。

## 0.3.24（2026-09-08）

9/8 下午又贡献了一个新案例：机器人网关插件用本地 tgz 方式重装，装的人把插件拷成了真实目录，但目录名写错了（目录叫 bot-gateway，包名其实是 dsh-bot-gateway——Node 是按目录名找包的，这么拷永远找不到），又在 profiles 层挂了个指向外部的桥接链接。开机时桌面版的链接清扫把桥接当"外来链接"删了，插件当场解析失败。排查时最难受的一点：清扫日志只写了数量（"1 foreign link(s)"），删的到底是谁，全靠文件时间戳反推。这版改四件事：一是清扫留名——删掉的每个链接的名字和目标都写进日志，不再让人猜；二是豁免清单从"只认 junction 安装"扩展到"手工拷进来的真实目录也算"，别再误伤自己人；三是启动预检新增裸包名解析检查——补丁里用包名引用的插件，开机前先验证能不能解析到，解析不到就提前处理，而且如果发现是"目录名和包名不一致"这种手滑，直接自动建一个名字正确的 junction 修好它，插件照常加载、不用禁用；四是隔离轮测期间不再执行"改补丁"类自愈和弹窗，也不在轮测中途隔离 profiles——上一版在这种场景里一口气隔离了两次，还顺手把排查要用的历史备份清掉了。四项改动都拿 9/8 的两起真实事故做了回归：早上的 ID 冲突照抓、不连坐，下午的解析失败自动修好，20 项断言全过。

## 0.3.23（2026-09-08）

9/8 开机那次自愈虽然把问题解决了，但走得太难看：加载器一次只报第一个错——web-fetch-http 修完重试才见到 comfyui-bridge，中间还白隔离了一遍 profiles；而且整块禁用把同一个块里无辜的插件也连坐了。这版把自愈升级成"启动预检"：开机先做一次全面比对，补丁条目对 DSH 内置清单、对官方安装登记（profile bundle）、对同文件重复声明，凡是会让冷启动炸掉的重复项一次全找出来、启动前就修掉，不用再一轮一轮崩。禁用粒度也从"整块"细化到"条目"：只注释冲突的那几行，同块的邻居插件不受牵连；注释保留原缩进，删掉行首的 # 就能恢复。用 9/8 的真实事故配置回归验证过：两个冲突一次全中，11 个无辜条目一个不少。

## 0.3.22（2026-09-06）

修一次"启动直接崩、自愈还全走弯路"的事故。起因：cordis.patch.yml 里手写的 `web-fetch-http` 条目和 DSH 内置的撞了 ID，另一个插件的前端条目和宿主入口撞了包名，后端加载插件树时直接崩。当时的自愈把 profile 隔离了一遍、又把一模一样的 Node 重下了一遍，全都白干，最后才弹错误窗口。现在这类配置冲突会在自愈第一步就被认出来：自动备份配置、只注释掉冲突的那几行、弹窗告诉你禁用了什么、立刻重试启动——两三秒就恢复。另外，插件配置加载类的失败会直接跳过"重装 Node"这步，不再浪费流量干不可能有用的事。

## 0.3.21（2026-09-04）

更新提示现在会两头核对：GitHub 上有、npm 上也发布了，两边都在才提示你更新。GitHub 跑得比 npm 快的时候（比如 GitHub 已经标到 0.1.3-alpha.1，npm 还没发包），就只提示两边都有的那个版本，并在日志里记一句"GitHub 已到 xxx，npm 还没发"。安装时手动指定的版本也会先查一遍 npm，没发布就直接说清楚，不再发起一场注定失败的下载。

## 0.3.20（2026-09-04）

加了个全量诊断日志：桌面上的 `DSH-Desktop-日志.txt`，启动和更新的每一步、每一个错误都实时写进去，连程序崩溃前的堆栈都会先落盘再弹窗。顺手修了个大坑——之前 GitHub 有新版本但 npm 还没发布时，下载明明失败了却弹"更新已下载完成"，害你白重启一次、版本纹丝不动还反复提示更新；现在下载失败会老老实实报错。另外托盘一创建就显示真实版本号（之前头 20 秒显示"读取中"，容易误以为更新没生效），首次版本检查也从 20 秒提前到 5 秒。

## 0.3.19（2026-09-04）

修"确认更新、也重启了、版本却没变"的问题。根因是重启太快：旧后端进程还没死透，新实例就急着替换目录，被文件锁卡住。现在重启前会等后端真正退出再拉起新实例；目录替换最多重试 4 次，不行就换复制的方式，全失败也保留着已下载的更新下次接着试，绝不丢。Node 更新失败误删暂存的问题也修了；安装时选的版本标记改成只用一次，不会每次启动都来插一脚。

## 0.3.18（2026-09-04）

更新这事不再自作主张。后台定时任务只负责"看一眼有没有新版"，有的话托盘气泡通知一声，不下载。想更新就点托盘里的版本号那几行，弹窗列出更新内容，你点了"下载并更新"才真正开始。下完问你是立刻重启还是稍后，选稍后就下次启动时生效。

## 0.3.17（2026-09-04）

修了一个开机就崩的 bug：插件逐个检查时主动停掉测试进程，却被当成"后端崩溃"处理，撞上一个没声明的变量，主进程直接弹窗崩掉，插件配置还留在半残状态。现在主动停止会先解绑监听，退出事件也只认"当前正在跑的那个实例"；隔离检查全程在启动页显示进度；核心起不来的话先把你的插件配置原样恢复再说。

## 0.3.16（2026-09-03）

修在线安装：之前选了"在线最新版本"，首次启动其实没把下载过程亮出来，还会继续用本地旧版凑合。现在老老实实先查版本、下载时启动页显示进度、应用完才启动。

## 0.3.15（2026-09-02）

安装时选后端版本多了一个选项："在线最新版本"——首次启动时联网拉当前最新的。适合永远想用最新后端的人。

## 0.3.14（2026-09-02）

版本选择页只在第一次安装时出现。覆盖安装、升级都不再问一遍——都升级了，谁还想重新选版本。

## 0.3.13（2026-09-02）

两个收敛：插件隔离只在"这次启动真的换了新后端"时才跑，平时启动不再反复隔离、反复重启后端；安装时选的版本也真正落实（下载、暂存并应用），不再只是记在纸上。

## 0.3.12（2026-09-02）

大更新后的安全启动：先把所有第三方插件注释掉，确认 DSH 本体起得来，再一个一个放回来试。有问题的保持禁用，启动后弹窗告诉你哪些插件不兼容。插件源码和原始配置都留着，随时能恢复。安装包也加了后端版本选择页。

## 0.3.11（2026-09-02）

对付老版本会话缓存：DSH 内核升级后，旧缓存缺两个新字段会被校验卡死。现在启动前自动补上默认值，改之前先备份、用临时文件替换，会话记录、配置、凭据一个都不碰。

## 0.3.10（2026-09-02）

修重启后 `dsh.new` 没换上去的问题：Windows 下目录改名偶尔会失败（杀毒软件、残留句柄都会捣乱），失败时改用复制替换，替换成功之前绝不删掉待应用的包。

## 0.3.9（2026-09-02）

托盘更新状态改成三行：当前版本、最新版本、更新进度/状态，一眼看明白。

## 0.3.8（2026-09-02）

托盘右键菜单加了更新状态：检查中、下载百分比、等重启、失败原因，下载进度同步显示在托盘上。

## 0.3.7（2026-09-02）

更新前先给插件拍快照；换新后端时留着旧版本，启动出问题可以回滚；坏插件单独禁用只改配置、不动源码，原配置自动备份。

## 0.3.6（2026-09-02）

更新判断改以 GitHub 上的版本为准（deepseek-ai/deepseek-harness 的 master），因为 npm 的 `latest` 标签经常滞后；GitHub 访问不了才回退 npm。预发布版本（alpha/beta/rc）的比较也补齐了。更新完托盘气泡会告诉你版本变化和上游最近改了什么。

## 0.3.5（2026-08-29）

DSH 默认保活：点右上角 X 只是缩到托盘，想真退出从托盘菜单来；托盘里还能开关开机自启。

## 0.3.4（2026-08-29）

外壳自愈加固：自动重建插件的 junction 链接，坏掉的补丁条目自动禁用（先备份原配置），不再因为一个插件起不来就整个瘫痪。

## 0.3.3（2026-08-28）

内部版本，未对外发布（混入了不打算公开的插件开发项目）。0.3.4 是 0.3.2 的直接后继。

## 0.3.2（2026-08-28）

安装器体验重做：一开始就请求提权（不再装到一半才弹 UAC）、并行解压提速、每个阶段都有进度提示；进程结束严格按路径匹配，绝不误杀你机器上其他的 node。

## 0.3.1（2026-08-28）

安装顺序固定为"先结束进程 → 卸载旧版 → 再装新版"，默认保留用户数据；环境变量净化、junction 自动修复、外壳/后端两套更新策略初步成型。

## 0.3.0（2026-08-28）

第一个版本：把 `dsh web` 包成 Windows 桌面应用。独立窗口、安装包、自带 Node 和完整后端、启动自愈、独立数据目录。
