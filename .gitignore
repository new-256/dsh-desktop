# ---------------------------------------------------------------------------
# Dependencies / toolchain
# ---------------------------------------------------------------------------
node_modules/

# ---------------------------------------------------------------------------
# Factory payload (regenerated by `npm install` -> scripts/install-backend.js
# + scripts/prepare-runtime.js). ~300 MB of third-party binaries: the bundled
# Node/npm runtime and the @deepseek-ai/dsh backend. Never commit these.
# ---------------------------------------------------------------------------
vendor/

# ---------------------------------------------------------------------------
# Packed payload parts (regenerated by scripts/pack-vendor.js from vendor/,
# which is itself ignored). ~50 MB of third-party binaries re-compressed into
# vendor-<n>.7z; a clean clone rebuilds them during `npm run dist`.
# ---------------------------------------------------------------------------
build/payload/

# ---------------------------------------------------------------------------
# Build output (electron-builder)
# ---------------------------------------------------------------------------
release/
dist/
*.exe
*.blockmap
latest.yml
builder-debug.yml
builder-effective-config.yaml

# ---------------------------------------------------------------------------
# Generated icons are kept in git on purpose (build/icon.ico, build/icon-256.png,
# assets/icon.png) so a clean clone can package without running make-icon.
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# Logs / caches / scratch
# ---------------------------------------------------------------------------
*.log
npm-debug.log*
.npm/
.eslintcache
_t.js
tmp/
.tmp/

# ---------------------------------------------------------------------------
# Local secrets / env files（密钥一律走环境变量，绝不入库）
# ---------------------------------------------------------------------------
.env
.env.*

# ---------------------------------------------------------------------------
# bot-gateway：研究用大压缩包（~147MB）与运行时状态不入库
# ---------------------------------------------------------------------------
bot-gateway/test/napcat-research/
bot-gateway/runtime-data/

# ---------------------------------------------------------------------------
# Editor / OS noise
# ---------------------------------------------------------------------------
.vscode/
.idea/
*.swp
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
.DS_Store
